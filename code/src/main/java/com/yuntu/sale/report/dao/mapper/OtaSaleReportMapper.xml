<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuntu.sale.report.dao.OtaSaleReportDao">

    <!-- 定义要查询的列 -->
	<sql id="queryColumns">
		id,
		name,
		is_delete,
		sort_no
	</sql>
    
    <!-- 查询 
    <select id="queryByName" parameterType="java.lang.String" resultType="com.yuntu.sale.product.po.ProductCategory">
        SELECT 
            <include refid="queryColumns"></include>
        FROM t_product_category 
        <where>
            is_delete = 0 
            <if test="name != null and name != ''">
                AND name like CONCAT('%', #{name}, '%') 
            </if>
        </where>
        ORDER BY sort_no 
    </select>-->

    
</mapper>